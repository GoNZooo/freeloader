freeloader
=====

A behaviour for HTTP downloads (freeloader_downloader)

Add to your deps
----------------

Add freeloader to your deps in rebar.config

    ...
    {deps, [...,
            freeloader,
            ...]}
    ...

Build
-----

    $ rebar3 compile
